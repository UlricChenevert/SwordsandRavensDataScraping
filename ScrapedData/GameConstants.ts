export const enum ReplacementReason {
  VOTE,
  CLOCK_TIMEOUT,
}

export enum HouseCardState {
    AVAILABLE,
    USED
}

export enum PlayerActionType {
  ORDERS_PLACED,
  BID_MADE,
  HOUSE_CARD_CHOSEN,
}