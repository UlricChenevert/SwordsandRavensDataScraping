import { BiddingTracker } from "./Bidding";
export const extractGameData = (logs) => {
    const logData = logs.map((log) => log.data);
    const Trackers = [BiddingTracker]();
};
//# sourceMappingURL=ExtractGameData.js.map